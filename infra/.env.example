# --- Database ---
POSTGRES_USER=evalitai
POSTGRES_PASSWORD=evalitai
POSTGRES_DB=evalitai
DATABASE_URL=postgresql+asyncpg://evalitai:evalitai@postgres:5432/evalitai

# --- Redis ---
REDIS_URL=redis://redis:6379

# --- MinIO (local S3) ---
MINIO_ROOT_USER=evalitai
MINIO_ROOT_PASSWORD=evalitai123
MINIO_BUCKET=evalitai
STORAGE_URL=http://minio:9000

# --- LLM Judge ---
# Add your provider key — only the one you want to use
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# --- App ---
# Optional: set a password to protect your instance
# If empty, the API is open (fine for local use)
EVALITAI_PASSWORD=

# Environment: development | production
ENVIRONMENT=development
